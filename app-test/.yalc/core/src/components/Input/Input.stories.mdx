import {
  Meta,
  Story,
  Props,
  Preview,
  Subtitle,
} from '@storybook/addon-docs/blocks';
// This isn't documented anywhere, but look into storybook docs exports: https://github.com/storybookjs/storybook/blob/master/addons/docs/src/blocks/index.ts

import { Input } from './Input';

<Meta title="Components/Input" component={Input} />

# Box

<Subtitle>
  The Box component is the root of all components and supports space, layout,
  flexbox, border, position, and color styled props from styled-system.
</Subtitle>

Building block of layouts. Creates a box-model context

<Preview withToolbar withSource="open">
  <Story name="Base">
    <Input />
  </Story>
</Preview>
