import {
  Meta,
  Story,
  Props,
  Preview,
  Subtitle,
} from '@storybook/addon-docs/blocks';
// This isn't documented anywhere, but look into storybook docs exports: https://github.com/storybookjs/storybook/blob/master/addons/docs/src/blocks/index.ts

import { Button } from './Button';

<Meta title="Components/Button" component={Button} />

# Button

<Subtitle>
  Button
</Subtitle>

<Preview withToolbar withSource="open">
  <Story name="Base">
      <Button>
        Button
      </Button>
  </Story>
</Preview>

<Preview withToolbar withSource="open">
  <Story name="With bare variant">
      <Button variant="bare">
        Bare Button
      </Button>
  </Story>
</Preview>

<Preview withToolbar withSource="open">
  <Story name="With primary variant">
      <Button variant="primary">
        Primary Button
      </Button>
  </Story>
</Preview>

<Preview withToolbar withSource="open">
  <Story name="With secondary variant">
      <Button variant="secondary">
        Secondary Button
      </Button>
  </Story>
</Preview>

<Preview withToolbar withSource="open">
  <Story name="With sx prop">
      <Button
        sx={{
          bg: 'primary',
        }}>
        Styled Button
      </Button>
  </Story>
</Preview>

