import {
  Meta,
  Story,
  Props,
  Preview,
  Subtitle,
} from '@storybook/addon-docs/blocks';
// This isn't documented anywhere, but look into storybook docs exports: https://github.com/storybookjs/storybook/blob/master/addons/docs/src/blocks/index.ts

import { Checkbox } from './Checkbox';
import { Flex } from '../Flex';
import { Label } from '../Label';

<Meta title="Components/Checkbox" component={Checkbox}></Meta>

# Checkbox

<Subtitle>Checkbox</Subtitle>

<Preview withToolbar withSource="open">
  <Story name="Base">
    <Checkbox label="Item"/>
  </Story>
</Preview>

If no `label` prop is passed to the `<Radio>` you *must* wrap the `<Radio>` with a `<label>` element. 

<Preview withToolbar withSource="open">
  <Story name="With no label">
    <Flex as={Label} htmlFor="checkbox-1">
      <Checkbox id="checkbox-1" name="example-1" mr={2} />
      Label 1
    </Flex>
    <Flex as={Label} htmlFor="checkbox-2">
      <Checkbox id="checkbox-2" name="example-2" mr={2} />
      Label 2
    </Flex>
  </Story>
</Preview>
